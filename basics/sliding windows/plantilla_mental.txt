int left = 0;

for (int right = 0; right < n; right++) {

    // 1. Agrego a[right]
    
    while (VENTANA_INVALIDA) {

        // 2. Quito a[left]
        left++;
    }

    // 3. La ventana ahora es válida
    // actualizo respuesta
}